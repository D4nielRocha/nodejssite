function Message(id = null, fname, lname, email, message) {

    this._id = id;       
    this.firstName = fname;    
    this.lastName = lname;    
    this.email = email;    
    this.message_text = message;    
    };